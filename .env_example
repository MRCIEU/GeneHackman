DATA_DIR=
RESULTS_DIR=
RDFS_DIR=
PIPELINE_DATA_DIR=/Users/wt23152/Documents/Projects/scratch/020/data/
DOCKER_VERSION=
SNAKEMAKE_PROFILE=snakemake/local/
PIPELINE_LOG_DIR=
# Optional: Lima VM name if apptainer only exists inside Lima (default: apptainer)
# GENEHACKMAN_LIMA_INSTANCE=apptainer

# Optional: directory for Snakemake to store pulled container images (docker:// when no local .sif).
# Default if unset: .snakemake/singularity under the directory where you run snakemake.
# GENEHACKMAN_SINGULARITY_PREFIX=/path/to/large/disk/snakemake_singularity
